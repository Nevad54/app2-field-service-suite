/* ========================================
   FIELD SERVICE SUITE - MODERN STYLES
   ======================================== */

:root {
  /* Light Theme */
  --bg-primary: #f8fafc;
  --bg-secondary: #ffffff;
  --bg-tertiary: #f1f5f9;
  --text-primary: #0f172a;
  --text-secondary: #475569;
  --text-muted: #94a3b8;
  --accent: #0ea5e9;
  --accent-hover: #0284c7;
  --accent-light: #e0f2fe;
  --success: #10b981;
  --success-light: #d1fae5;
  --warning: #f59e0b;
  --warning-light: #fef3c7;
  --danger: #ef4444;
  --danger-light: #fee2e2;
  --border: #e2e8f0;
  --shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -2px rgba(0, 0, 0, 0.1);
  --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -4px rgba(0, 0, 0, 0.1);
  --radius: 12px;
  --radius-sm: 8px;
}

[data-theme="dark"] {
  /* Dark Theme */
  --bg-primary: #0f172a;
  --bg-secondary: #1e293b;
  --bg-tertiary: #334155;
  --text-primary: #f1f5f9;
  --text-secondary: #cbd5e1;
  --text-muted: #64748b;
  --accent: #38bdf8;
  --accent-hover: #0ea5e9;
  --accent-light: #1e3a5f;
  --success: #34d399;
  --success-light: #064e3b;
  --warning: #fbbf24;
  --warning-light: #451a03;
  --danger: #f87171;
  --danger-light: #450a0a;
  --border: #334155;
  --shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.3);
  --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.4);
}

* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

body {
  font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
  background: var(--bg-primary);
  color: var(--text-primary);
  line-height: 1.6;
  min-height: 100vh;
}

/* ========================================
   LAYOUT
   ======================================== */
.app-shell {
  min-height: 100vh;
  background: linear-gradient(135deg, var(--bg-primary) 0%, var(--bg-tertiary) 100%);
}

.main {
  max-width: 1200px;
  margin: 0 auto;
  padding: 32px 20px;
}

/* ========================================
   TOPBAR
   ======================================== */
.topbar {
  position: sticky;
  top: 0;
  z-index: 100;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 8px 16px;
  background: rgba(255, 255, 255, 0.98);
  -webkit-backdrop-filter: blur(12px);
  backdrop-filter: blur(12px);
  border-bottom: 1px solid var(--border);
  box-shadow: var(--shadow);
  flex-wrap: wrap;
  gap: 8px;
  min-height: 56px;
}

[data-theme="dark"] .topbar {
  background: rgba(30, 41, 59, 0.98);
}

.brand-section {
  display: flex;
  align-items: center;
  gap: 8px;
  flex-shrink: 0;
}

.brand {
  font-size: 1.1rem;
  font-weight: 800;
  background: linear-gradient(135deg, var(--accent) 0%, #8b5cf6 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  text-decoration: none;
  letter-spacing: -0.5px;
  white-space: nowrap;
}

.nav-links {
  display: flex;
  gap: 2px;
  flex-wrap: wrap;
  justify-content: center;
  flex: 1;
  margin: 0 12px;
  overflow: hidden;
}

.nav-links a {
  text-decoration: none;
  color: var(--text-secondary);
  padding: 6px 10px;
  border-radius: var(--radius-sm);
  font-weight: 500;
  font-size: 0.8rem;
  transition: all 0.2s ease;
  white-space: nowrap;
}

.nav-links a:hover {
  background: var(--accent-light);
  color: var(--accent);
}

.nav-links a.active {
  background: var(--accent);
  color: white;
  box-shadow: 0 2px 8px rgba(14, 165, 233, 0.3);
}

.user-section {
  display: flex;
  align-items: center;
  gap: 8px;
  flex-shrink: 0;
}

.user-menu {
  display: flex;
  align-items: center;
  gap: 6px;
  padding: 4px 8px;
  background: var(--bg-tertiary);
  border-radius: var(--radius-sm);
  font-size: 0.8rem;
}

.user-name {
  font-weight: 600;
  color: var(--text-primary);
  font-size: 0.75rem;
}

.user-role {
  font-size: 0.65rem;
  padding: 2px 6px;
  background: var(--accent-light);
  color: var(--accent);
  border-radius: 8px;
  text-transform: capitalize;
}

/* Dark mode toggle */
.dark-mode-toggle {
  font-size: 1.1rem;
  transition: all 0.3s ease;
  padding: 4px;
}

.dark-mode-toggle:hover {
  transform: scale(1.1);
}

.notifications-btn {
  position: relative;
  padding: 4px;
}

.notification-badge {
  position: absolute;
  top: -2px;
  right: -2px;
  background: var(--danger);
  color: white;
  font-size: 0.6rem;
  font-weight: 700;
  padding: 1px 4px;
  border-radius: 8px;
  min-width: 14px;
  text-align: center;
}

/* ========================================
   BUTTONS
   ======================================== */
.btn-primary,
.btn-secondary,
.btn-success,
.btn-danger,
.btn-icon {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 6px;
  padding: 10px 18px;
  border-radius: var(--radius-sm);
  font-weight: 600;
  font-size: 0.9rem;
  cursor: pointer;
  transition: all 0.2s ease;
  border: none;
  text-decoration: none;
}

.btn-primary {
  background: linear-gradient(135deg, var(--accent) 0%, var(--accent-hover) 100%);
  color: white;
  box-shadow: 0 2px 8px rgba(14, 165, 233, 0.3);
}

.btn-primary:hover {
  transform: translateY(-1px);
  box-shadow: 0 4px 12px rgba(14, 165, 233, 0.4);
}

.btn-secondary {
  background: var(--bg-secondary);
  color: var(--text-primary);
  border: 1px solid var(--border);
}

.btn-secondary:hover {
  background: var(--bg-tertiary);
  border-color: var(--accent);
}

.btn-success {
  background: linear-gradient(135deg, var(--success) 0%, #059669 100%);
  color: white;
}

.btn-success:hover {
  transform: translateY(-1px);
  box-shadow: 0 4px 12px rgba(16, 185, 129, 0.4);
}

.btn-danger {
  background: var(--danger-light);
  color: var(--danger);
  border: 1px solid var(--danger);
}

.btn-danger:hover {
  background: var(--danger);
  color: white;
}

.btn-icon {
  width: 40px;
  height: 40px;
  padding: 0;
  background: var(--bg-tertiary);
  border-radius: 50%;
  font-size: 1.1rem;
}

.btn-icon:hover {
  background: var(--accent-light);
}

.btn-full {
  width: 100%;
}

/* ========================================
   CARDS
   ======================================== */
.card {
  background: var(--bg-secondary);
  border-radius: var(--radius);
  padding: 28px;
  box-shadow: var(--shadow);
  border: 1px solid var(--border);
  margin-bottom: 24px;
}

.card h1 {
  font-size: 1.8rem;
  font-weight: 700;
  margin-bottom: 8px;
  color: var(--text-primary);
}

.card h2 {
  font-size: 1.3rem;
  font-weight: 600;
  margin-bottom: 16px;
  color: var(--text-primary);
}

/* ========================================
   PAGE HEADER
   ======================================== */
.page-header {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  margin-bottom: 24px;
  flex-wrap: wrap;
  gap: 16px;
}

.page-header h1 {
  display: flex;
  align-items: center;
  gap: 12px;
}

/* ========================================
   FORMS
   ======================================== */
.form-section {
  background: var(--bg-tertiary);
  border-radius: var(--radius);
  padding: 24px;
  margin-bottom: 24px;
  border: 1px solid var(--border);
}

.form-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  gap: 16px;
}

.form-group {
  display: flex;
  flex-direction: column;
  gap: 6px;
}

.form-group.full-width {
  grid-column: 1 / -1;
}

.form-group label {
  font-size: 0.85rem;
  font-weight: 600;
  color: var(--text-secondary);
}

.form-group input,
.form-group select,
.form-group textarea {
  padding: 12px 14px;
  border: 2px solid var(--border);
  border-radius: var(--radius-sm);
  font-size: 0.95rem;
  background: var(--bg-secondary);
  color: var(--text-primary);
  transition: all 0.2s ease;
}

.form-group input:focus,
.form-group select:focus,
.form-group textarea:focus {
  outline: none;
  border-color: var(--accent);
  box-shadow: 0 0 0 3px var(--accent-light);
}

.form-actions {
  display: flex;
  gap: 12px;
  margin-top: 20px;
}

.form-error-box {
  background: var(--danger-light);
  color: var(--danger);
  padding: 12px 16px;
  border-radius: var(--radius-sm);
  margin-bottom: 16px;
  font-weight: 500;
  border-left: 4px solid var(--danger);
}

/* Time picker in job form */
.time-picker-group {
  display: flex;
  gap: 12px;
}

.time-picker-group .form-group {
  flex: 1;
}

.time-picker-group input[type="time"] {
  padding: 12px;
  border: 2px solid var(--border);
  border-radius: var(--radius-sm);
  background: var(--bg-secondary);
  color: var(--text-primary);
  font-size: 0.95rem;
}

.time-picker-group input[type="time"]:focus {
  outline: none;
  border-color: var(--accent);
}

/* ========================================
   SEARCH & FILTERS
   ======================================== */
.search-bar,
.search-filter-bar {
  display: flex;
  gap: 12px;
  margin-bottom: 20px;
  flex-wrap: wrap;
}

.search-bar input,
.search-filter-bar input {
  flex: 1;
  min-width: 200px;
  padding: 12px 16px;
  border: 2px solid var(--border);
  border-radius: var(--radius-sm);
  font-size: 0.95rem;
  background: var(--bg-secondary);
  color: var(--text-primary);
}

.search-bar input:focus,
.search-filter-bar input:focus {
  outline: none;
  border-color: var(--accent);
}

.search-filter-bar select {
  padding: 12px 16px;
  border: 2px solid var(--border);
  border-radius: var(--radius-sm);
  font-size: 0.95rem;
  background: var(--bg-secondary);
  color: var(--text-primary);
  min-width: 150px;
}

.search-count {
  display: flex;
  align-items: center;
  padding: 0 12px;
  color: var(--text-muted);
  font-size: 0.9rem;
}

/* ========================================
   STATS GRID
   ======================================== */
.stats-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
  gap: 16px;
  margin-bottom: 24px;
}

.stat-card {
  background: var(--bg-secondary);
  border-radius: var(--radius);
  padding: 20px;
  display: flex;
  align-items: center;
  gap: 16px;
  border: 1px solid var(--border);
  transition: all 0.2s ease;
}

.stat-card:hover {
  transform: translateY(-2px);
  box-shadow: var(--shadow-lg);
}

.stat-card.large {
  grid-column: span 2;
  background: linear-gradient(135deg, var(--accent-light) 0%, var(--bg-secondary) 100%);
}

.stat-icon {
  font-size: 2rem;
  width: 56px;
  height: 56px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--accent-light);
  border-radius: var(--radius);
}

.stat-info {
  display: flex;
  flex-direction: column;
}

.stat-value {
  font-size: 1.8rem;
  font-weight: 800;
  color: var(--text-primary);
  line-height: 1.2;
}

.stat-label {
  font-size: 0.85rem;
  color: var(--text-muted);
  font-weight: 500;
}

.stat-card.completed .stat-icon { background: var(--success-light); }
.stat-card.progress .stat-icon { background: var(--warning-light); }
.stat-card.revenue .stat-icon { background: #fef3c7; }
.stat-card.pending .stat-icon { background: #fce7f3; }

/* ========================================
   ITEMS LIST (Jobs, Customers)
   ======================================== */
.items-grid,
.jobs-list {
  display: grid;
  gap: 16px;
}

.item-card,
.job-item {
  background: var(--bg-secondary);
  border-radius: var(--radius);
  padding: 20px;
  border: 1px solid var(--border);
  transition: all 0.2s ease;
}

.item-card:hover,
.job-item:hover {
  box-shadow: var(--shadow-lg);
  border-color: var(--accent);
}

.item-header,
.job-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 12px;
}

.item-header h3,
.job-header h3 {
  font-size: 1.1rem;
  font-weight: 600;
}

.item-id,
.category-tag {
  font-size: 0.75rem;
  padding: 4px 10px;
  background: var(--bg-tertiary);
  color: var(--text-muted);
  border-radius: 20px;
  font-weight: 600;
}

.category-tag {
  text-transform: uppercase;
  background: var(--accent-light);
  color: var(--accent);
  font-size: 0.7rem;
}

.category-tag.hvac { background: #dbeafe; color: #2563eb; }
.category-tag.electrical { background: #fef3c7; color: #d97706; }
.category-tag.plumbing { background: #d1fae5; color: #059669; }
.category-tag.repair { background: #fce7f3; color: #db2777; }
.category-tag.installation { background: #e0e7ff; color: #4f46e5; }
.category-tag.inspection { background: #f3e8ff; color: #9333ea; }

/* Dark mode category tags */
[data-theme="dark"] .category-tag.hvac { background: #1e3a5f; color: #60a5fa; }
[data-theme="dark"] .category-tag.electrical { background: #451a03; color: #fbbf24; }
[data-theme="dark"] .category-tag.plumbing { background: #064e3b; color: #34d399; }
[data-theme="dark"] .category-tag.repair { background: #500724; color: #f472b6; }
[data-theme="dark"] .category-tag.installation { background: #312e81; color: #818cf8; }
[data-theme="dark"] .category-tag.inspection { background: #4c1d95; color: #c084fc; }

.item-details p {
  margin: 4px 0;
  font-size: 0.9rem;
  color: var(--text-secondary);
}

.item-actions {
  display: flex;
  gap: 8px;
  margin-top: 16px;
}

/* Job counts */
.job-counts {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
}

.job-counts .count {
  font-size: 0.85rem;
  padding: 4px 12px;
  border-radius: 20px;
  background: var(--bg-tertiary);
  color: var(--text-secondary);
}

.job-counts .count.total { background: var(--accent-light); color: var(--accent); }
.job-counts .count.new { background: #f1f5f9; color: #64748b; }
.job-counts .count.progress { background: #ffedd5; color: #ea580c; }
.job-counts .count.completed { background: #d1fae5; color: #059669; }

[data-theme="dark"] .job-counts .count.new { background: #334155; color: #94a3b8; }
[data-theme="dark"] .job-counts .count.progress { background: #451a03; color: #fb923c; }
[data-theme="dark"] .job-counts .count.completed { background: #064e3b; color: #34d399; }

/* ========================================
   STATUS BADGES
   ======================================== */
.status-badge {
  display: inline-flex;
  align-items: center;
  padding: 4px 12px;
  border-radius: 20px;
  font-size: 0.75rem;
  font-weight: 700;
  text-transform: uppercase;
}

.status-new { background: #f1f5f9; color: #64748b; }
.status-assigned { background: #dbeafe; color: #2563eb; }
.status-progress, .status-in-progress { background: #ffedd5; color: #ea580c; }
.status-completed { background: #d1fae5; color: #059669; }

[data-theme="dark"] .status-new { background: #334155; color: #94a3b8; }
[data-theme="dark"] .status-assigned { background: #1e3a5f; color: #60a5fa; }
[data-theme="dark"] .status-progress { background: #451a03; color: #fb923c; }
[data-theme="dark"] .status-completed { background: #064e3b; color: #34d399; }

/* ========================================
   INVOICES
   ======================================== */
.invoice-summary {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
  gap: 16px;
  margin-bottom: 24px;
}

.summary-card {
  background: var(--bg-secondary);
  border-radius: var(--radius);
  padding: 20px;
  text-align: center;
  border: 1px solid var(--border);
}

.summary-label {
  display: block;
  font-size: 0.85rem;
  color: var(--text-muted);
  margin-bottom: 8px;
}

.summary-value {
  font-size: 1.6rem;
  font-weight: 800;
  color: var(--text-primary);
}

.invoice-card {
  background: var(--bg-secondary);
  border-radius: var(--radius);
  padding: 20px;
  border: 1px solid var(--border);
}

.invoice-card.pending { border-left: 4px solid var(--warning); }
.invoice-card.paid { border-left: 4px solid var(--success); }
.invoice-card.cancelled { border-left: 4px solid #64748b; opacity: 0.7; }

.invoice-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 12px;
}

.invoice-amount {
  font-size: 1.4rem;
  font-weight: 700;
  color: var(--accent);
}

.invoice-status {
  padding: 4px 12px;
  border-radius: 20px;
  font-size: 0.75rem;
  font-weight: 700;
}

.invoice-status.pending { background: var(--warning-light); color: #b45309; }
.invoice-status.paid { background: var(--success-light); color: #047857; }
.invoice-status.cancelled { background: #f1f5f9; color: #64748b; }

.invoice-actions {
  display: flex;
  gap: 8px;
  margin-top: 16px;
  flex-wrap: wrap;
}

/* ========================================
   SCHEDULE / CALENDAR
   ======================================== */
.view-toggle {
  display: flex;
  gap: 4px;
  background: var(--bg-tertiary);
  padding: 4px;
  border-radius: var(--radius-sm);
}

.toggle-btn {
  padding: 8px 16px;
  border: none;
  background: transparent;
  border-radius: 6px;
  cursor: pointer;
  font-weight: 500;
  color: var(--text-muted);
  transition: all 0.2s ease;
}

.toggle-btn.active {
  background: var(--bg-secondary);
  color: var(--accent);
  box-shadow: var(--shadow);
}

.schedule-date-group {
  margin-bottom: 24px;
}

.schedule-date {
  font-size: 1.1rem;
  font-weight: 600;
  color: var(--text-primary);
  margin-bottom: 12px;
  padding-bottom: 8px;
  border-bottom: 2px solid var(--border);
}

.schedule-items {
  display: grid;
  gap: 12px;
}

.schedule-item {
  display: flex;
  gap: 16px;
  padding: 16px;
  background: var(--bg-secondary);
  border-radius: var(--radius-sm);
  border: 1px solid var(--border);
  align-items: center;
}

.schedule-item.status-new { border-left: 4px solid #64748b; }
.schedule-item.status-assigned { border-left: 4px solid #2563eb; }
.schedule-item.status-in-progress { border-left: 4px solid #ea580c; }
.schedule-item.status-completed { border-left: 4px solid #059669; }

.schedule-content {
  flex: 1;
}

.schedule-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 4px;
}

.schedule-title {
  color: var(--text-secondary);
  font-size: 0.9rem;
}

.schedule-meta {
  display: flex;
  gap: 12px;
  font-size: 0.85rem;
  color: var(--text-muted);
  margin-top: 8px;
}

/* Calendar Grid */
.calendar-view {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
  gap: 12px;
}

.calendar-day {
  background: var(--bg-secondary);
  border-radius: var(--radius-sm);
  padding: 12px;
  border: 1px solid var(--border);
  min-height: 100px;
}

.calendar-day-header {
  text-align: center;
  margin-bottom: 8px;
  padding-bottom: 8px;
  border-bottom: 1px solid var(--border);
}

.day-number {
  display: block;
  font-size: 1.4rem;
  font-weight: 700;
  color: var(--accent);
}

.day-name {
  font-size: 0.75rem;
  color: var(--text-muted);
  text-transform: uppercase;
}

.calendar-job {
  font-size: 0.75rem;
  padding: 4px 8px;
  background: var(--accent-light);
  border-radius: 4px;
  margin-bottom: 4px;
  color: var(--accent);
}

/* Gantt Chart View - Table Style */
.gantt-view {
  overflow-x: auto;
}

.gantt-chart {
  min-width: 800px;
  background: var(--bg-secondary);
  border-radius: var(--radius);
  border: 1px solid var(--border);
  overflow: hidden;
}

.gantt-header {
  display: flex;
  background: var(--bg-tertiary);
  border-bottom: 2px solid var(--border);
}

.gantt-col {
  padding: 14px 12px;
  font-weight: 600;
  font-size: 0.85rem;
  color: var(--text-secondary);
  border-right: 1px solid var(--border);
}

.gantt-col:last-child {
  border-right: none;
}

.gantt-col.job-col { min-width: 180px; flex: 1.5; }
.gantt-col.customer-col { min-width: 140px; flex: 1.2; }
.gantt-col.status-col { min-width: 100px; }
.gantt-col.priority-col { min-width: 80px; }
.gantt-col.date-col { min-width: 120px; }
.gantt-col.location-col { min-width: 150px; flex: 1.2; }

.gantt-body {
  max-height: 500px;
  overflow-y: auto;
}

.gantt-row {
  display: flex;
  border-bottom: 1px solid var(--border);
  transition: background 0.2s ease;
}

.gantt-row:hover {
  background: var(--bg-tertiary);
}

.gantt-row:last-child {
  border-bottom: none;
}

.gantt-row .gantt-col {
  font-size: 0.9rem;
  color: var(--text-primary);
  display: flex;
  align-items: center;
}

.gantt-job-id {
  font-family: 'Courier New', monospace;
  font-size: 0.8rem;
  background: var(--bg-tertiary);
  padding: 2px 6px;
  border-radius: 4px;
  margin-right: 8px;
}

.gantt-job-title {
  font-weight: 500;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.gantt-time {
  display: block;
  font-size: 0.75rem;
  color: var(--text-muted);
  margin-top: 2px;
}

.priority-badge {
  display: inline-block;
  padding: 2px 8px;
  border-radius: 10px;
  font-size: 0.7rem;
  font-weight: 600;
  text-transform: uppercase;
}

.priority-badge.high { background: #fee2e2; color: #dc2626; }
.priority-badge.medium { background: #fef3c7; color: #d97706; }
.priority-badge.low { background: #f1f5f9; color: #64748b; }

[data-theme="dark"] .priority-badge.high { background: #450a0a; color: #f87171; }
[data-theme="dark"] .priority-badge.medium { background: #451a03; color: #fbbf24; }
[data-theme="dark"] .priority-badge.low { background: #334155; color: #94a3b8; }

/* ========================================
   PROJECT PLANNER - SPREADSHEET STYLE
   ======================================== */
.project-planner {
  background: var(--bg-secondary);
  border-radius: var(--radius);
  padding: 24px;
  border: 1px solid var(--border);
}

.planner-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 24px;
  flex-wrap: wrap;
  gap: 16px;
}

.planner-title {
  display: flex;
  align-items: center;
  gap: 16px;
}

.planner-title h1 {
  font-size: 1.5rem;
  margin: 0;
}

.planner-title .project-title {
  font-size: 1.1rem;
  color: var(--accent);
  font-weight: 600;
  padding: 6px 12px;
  background: var(--accent-light);
  border-radius: var(--radius-sm);
}

/* Summary Panel */
.planner-summary {
  display: flex;
  align-items: center;
  gap: 24px;
  padding: 20px;
  background: var(--bg-tertiary);
  border-radius: var(--radius);
  margin-bottom: 24px;
  flex-wrap: wrap;
}

.planner-summary .summary-item {
  flex: 1;
  min-width: 200px;
}

.planner-summary .summary-label {
  display: block;
  font-size: 0.85rem;
  color: var(--text-muted);
  margin-bottom: 8px;
}

.planner-summary .summary-progress {
  display: flex;
  align-items: center;
  gap: 12px;
}

.planner-summary .progress-bar {
  flex: 1;
  height: 12px;
}

.planner-summary .progress-bar.large {
  height: 16px;
  border-radius: 8px;
}

.planner-summary .summary-value {
  font-size: 1.2rem;
  font-weight: 700;
  color: var(--accent);
  min-width: 50px;
}

.summary-stats {
  display: flex;
  gap: 16px;
  flex-wrap: wrap;
}

.stat-mini {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 12px 16px;
  background: var(--bg-secondary);
  border-radius: var(--radius-sm);
  min-width: 60px;
}

.stat-mini .stat-num {
  font-size: 1.3rem;
  font-weight: 700;
  color: var(--text-primary);
}

.stat-mini .stat-lbl {
  font-size: 0.75rem;
  color: var(--text-muted);
}

.stat-mini.completed .stat-num { color: var(--success); }
.stat-mini.progress .stat-num { color: var(--warning); }
.stat-mini.delayed .stat-num { color: var(--danger); }

.summary-formula {
  display: flex;
  flex-direction: column;
  gap: 4px;
  padding: 12px;
  background: var(--accent-light);
  border-radius: var(--radius-sm);
  font-size: 0.85rem;
}

.summary-formula .formula {
  font-weight: 600;
  color: var(--accent);
}

.summary-formula .formula-value {
  color: var(--text-secondary);
  font-family: monospace;
}

/* Spreadsheet View */
.spreadsheet-view {
  border: 1px solid var(--border);
  border-radius: var(--radius);
  overflow: hidden;
}

.spreadsheet-toolbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 12px 16px;
  background: var(--bg-tertiary);
  border-bottom: 1px solid var(--border);
  flex-wrap: wrap;
  gap: 12px;
}

.toolbar-left {
  display: flex;
  align-items: center;
  gap: 8px;
}

.toolbar-left label {
  font-size: 0.9rem;
  color: var(--text-secondary);
}

.toolbar-left select {
  padding: 8px 12px;
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  background: var(--bg-secondary);
  color: var(--text-primary);
}

/* Spreadsheet Table */
.spreadsheet-table {
  overflow-x: auto;
}

.spreadsheet-header {
  display: flex;
  background: var(--bg-tertiary);
  border-bottom: 2px solid var(--border);
  min-width: 900px;
}

.spreadsheet-body {
  max-height: 500px;
  overflow-y: auto;
}

.spreadsheet-row {
  display: flex;
  border-bottom: 1px solid var(--border);
  min-width: 900px;
  transition: background 0.2s;
}

.spreadsheet-row:hover {
  background: var(--bg-tertiary);
}

.spreadsheet-row.delayed {
  background: var(--danger-light);
}

.spreadsheet-cell {
  padding: 10px 8px;
  border-right: 1px solid var(--border);
  display: flex;
  align-items: center;
  font-size: 0.9rem;
}

.spreadsheet-cell:last-child {
  border-right: none;
}

.spreadsheet-cell.header-cell {
  font-weight: 600;
  color: var(--text-secondary);
  text-transform: uppercase;
  font-size: 0.75rem;
  background: var(--bg-tertiary);
}

.spreadsheet-cell.readonly {
  background: var(--bg-tertiary);
  color: var(--text-muted);
}

.spreadsheet-cell.editable {
  cursor: pointer;
}

.spreadsheet-cell.editable:hover {
  background: var(--accent-light);
}

.spreadsheet-cell input,
.spreadsheet-cell select {
  width: 100%;
  padding: 6px 8px;
  border: 2px solid var(--accent);
  border-radius: 4px;
  background: var(--bg-secondary);
  color: var(--text-primary);
  font-size: 0.9rem;
}

.spreadsheet-cell.calculated {
  background: var(--bg-tertiary);
  font-weight: 600;
  color: var(--accent);
}

/* Cell sizes */
.cell-index { width: 40px; min-width: 40px; justify-content: center; }
.cell-name { width: 180px; min-width: 180px; }
.cell-start-date { width: 120px; min-width: 120px; }
.cell-end-date { width: 120px; min-width: 120px; }
.cell-duration { width: 80px; min-width: 80px; justify-content: center; }
.cell-progress { width: 80px; min-width: 80px; justify-content: center; }
.cell-status { width: 100px; min-width: 100px; justify-content: center; }
.cell-weight { width: 60px; min-width: 60px; justify-content: center; }
.cell-contribution { width: 90px; min-width: 90px; justify-content: center; }
.cell-notes { flex: 1; min-width: 150px; }
.cell-actions { width: 50px; min-width: 50px; justify-content: center; }

.btn-delete {
  padding: 4px;
  font-size: 0.9rem;
  background: transparent;
  border: none;
  cursor: pointer;
  opacity: 0.6;
}

.btn-delete:hover {
  opacity: 1;
}

.placeholder {
  color: var(--text-muted);
  font-style: italic;
}

/* Status badges */
.status-delayed {
  background: var(--danger-light);
  color: var(--danger);
}

[data-theme="dark"] .status-delayed {
  background: #450a0a;
  color: #f87171;
}

/* Empty state */
.empty-spreadsheet {
  padding: 48px;
  text-align: center;
  color: var(--text-muted);
}

/* Formula reference */
.formula-reference {
  padding: 16px;
  background: var(--bg-tertiary);
  border-top: 1px solid var(--border);
}

.formula-reference h4 {
  margin-bottom: 12px;
  font-size: 0.9rem;
}

.formula-reference ul {
  list-style: none;
  padding: 0;
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 8px;
}

.formula-reference li {
  font-size: 0.85rem;
  color: var(--text-secondary);
}

.formula-reference strong {
  color: var(--accent);
}

/* Add Task Form */
.add-task-form {
  padding: 20px;
  background: var(--bg-tertiary);
  border-bottom: 1px solid var(--border);
}

.add-task-form h3 {
  margin-bottom: 16px;
}

.add-task-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
  gap: 16px;
  margin-bottom: 16px;
}

/* Timeline View */
.timeline-view {
  padding: 20px;
}

.timeline-grid {
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.timeline-row {
  padding: 16px;
  background: var(--bg-tertiary);
  border-radius: var(--radius-sm);
  border-left: 4px solid var(--border);
}

.timeline-row.completed {
  border-left-color: var(--success);
}

.timeline-row.in_progress {
  border-left-color: var(--warning);
}

.timeline-row.delayed {
  border-left-color: var(--danger);
}

.timeline-row.not_started {
  border-left-color: var(--text-muted);
}

.timeline-task-name {
  font-weight: 600;
  margin-bottom: 8px;
  color: var(--text-primary);
}

.timeline-dates {
  display: flex;
  gap: 16px;
  font-size: 0.85rem;
  color: var(--text-muted);
  margin-bottom: 12px;
}

.timeline-duration {
  font-weight: 600;
  color: var(--accent);
}

.timeline-bar-container {
  height: 24px;
  background: var(--bg-secondary);
  border-radius: 12px;
  overflow: hidden;
  margin-bottom: 8px;
}

.timeline-bar {
  height: 100%;
  border-radius: 12px;
  display: flex;
  align-items: center;
  justify-content: flex-end;
  padding-right: 8px;
  min-width: 40px;
}

.timeline-bar.completed { background: var(--success); color: white; }
.timeline-bar.in_progress { background: var(--warning); color: white; }
.timeline-bar.delayed { background: var(--danger); color: white; }
.timeline-bar.not_started { background: var(--text-muted); color: white; }

.timeline-progress-label {
  font-size: 0.75rem;
  font-weight: 600;
}

.timeline-status {
  font-size: 0.85rem;
  font-weight: 500;
}

.timeline-status.completed { color: var(--success); }
.timeline-status.in_progress { color: var(--warning); }
.timeline-status.delayed { color: var(--danger); }
.timeline-status.not_started { color: var(--text-muted); }

/* Summary View */
.summary-view {
  padding: 20px;
}

.summary-panel {
  max-width: 600px;
  margin: 0 auto;
}

.summary-panel h3 {
  text-align: center;
  margin-bottom: 24px;
}

.summary-section {
  margin-bottom: 24px;
  padding: 20px;
  background: var(--bg-tertiary);
  border-radius: var(--radius);
}

.summary-section h4 {
  margin-bottom: 16px;
  font-size: 1rem;
}

/* Progress Circle */
.progress-circle-container {
  position: relative;
  width: 150px;
  height: 150px;
  margin: 0 auto;
}

.progress-circle {
  transform: rotate(-90deg);
}

.progress-circle .progress-bg {
  fill: none;
  stroke: var(--bg-secondary);
  stroke-width: 8;
}

.progress-circle .progress-fill {
  fill: none;
  stroke: var(--accent);
  stroke-width: 8;
  stroke-linecap: round;
  transition: stroke-dasharray 0.5s ease;
}

.progress-circle-text {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  text-align: center;
}

.progress-percent {
  display: block;
  font-size: 2rem;
  font-weight: 700;
  color: var(--text-primary);
}

.progress-label {
  font-size: 0.85rem;
  color: var(--text-muted);
}

/* Breakdown List */
.breakdown-list {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.breakdown-item {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 12px;
  background: var(--bg-secondary);
  border-radius: var(--radius-sm);
}

.breakdown-icon {
  font-size: 1.2rem;
}

.breakdown-label {
  flex: 1;
}

.breakdown-value {
  font-weight: 700;
  font-size: 1.1rem;
}

.breakdown-item.completed .breakdown-value { color: var(--success); }
.breakdown-item.in-progress .breakdown-value { color: var(--warning); }
.breakdown-item.delayed .breakdown-value { color: var(--danger); }
.breakdown-item.not-started .breakdown-value { color: var(--text-muted); }

/* Calc Details */
.calc-details {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.calc-row {
  display: flex;
  justify-content: space-between;
  padding: 8px;
  background: var(--bg-secondary);
  border-radius: 4px;
}

.calc-row.formula {
  flex-direction: column;
  gap: 4px;
  font-family: monospace;
  font-size: 0.9rem;
  background: var(--accent-light);
}

/* Responsive */
@media (max-width: 768px) {
  .planner-header {
    flex-direction: column;
    align-items: flex-start;
  }
  
  .planner-summary {
    flex-direction: column;
  }
  
  .spreadsheet-header,
  .spreadsheet-row {
    min-width: 700px;
  }
}

/* ========================================
   PROJECT PLANNER - SPREADSHEET STYLE
   ======================================== */
.project-planner {
  background: var(--bg-secondary);
  border-radius: var(--radius);
  padding: 24px;
  border: 1px solid var(--border);
}

.planner-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 24px;
  flex-wrap: wrap;
  gap: 16px;
}

.planner-title {
  display: flex;
  align-items: center;
  gap: 16px;
}

.planner-title h1 {
  font-size: 1.5rem;
  margin: 0;
}

.planner-title .project-title {
  font-size: 1.1rem;
  color: var(--accent);
  font-weight: 600;
  padding: 6px 12px;
  background: var(--accent-light);
  border-radius: var(--radius-sm);
}

/* Summary Panel */
.planner-summary {
  display: flex;
  align-items: center;
  gap: 24px;
  padding: 20px;
  background: var(--bg-tertiary);
  border-radius: var(--radius);
  margin-bottom: 24px;
  flex-wrap: wrap;
}

.planner-summary .summary-item {
  flex: 1;
  min-width: 200px;
}

.planner-summary .summary-label {
  display: block;
  font-size: 0.85rem;
  color: var(--text-muted);
  margin-bottom: 8px;
}

.planner-summary .summary-progress {
  display: flex;
  align-items: center;
  gap: 12px;
}

.planner-summary .progress-bar {
  flex: 1;
  height: 12px;
}

.planner-summary .progress-bar.large {
  height: 16px;
  border-radius: 8px;
}

.planner-summary .summary-value {
  font-size: 1.2rem;
  font-weight: 700;
  color: var(--accent);
  min-width: 50px;
}

.summary-stats {
  display: flex;
  gap: 16px;
  flex-wrap: wrap;
}

.stat-mini {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 12px 16px;
  background: var(--bg-secondary);
  border-radius: var(--radius-sm);
  min-width: 60px;
}

.stat-mini .stat-num {
  font-size: 1.3rem;
  font-weight: 700;
  color: var(--text-primary);
}

.stat-mini .stat-lbl {
  font-size: 0.75rem;
  color: var(--text-muted);
}

.stat-mini.completed .stat-num { color: var(--success); }
.stat-mini.progress .stat-num { color: var(--warning); }
.stat-mini.delayed .stat-num { color: var(--danger); }

.summary-formula {
  display: flex;
  flex-direction: column;
  gap: 4px;
  padding: 12px;
  background: var(--accent-light);
  border-radius: var(--radius-sm);
  font-size: 0.85rem;
}

.summary-formula .formula {
  font-weight: 600;
  color: var(--accent);
}

.summary-formula .formula-value {
  color: var(--text-secondary);
  font-family: monospace;
}

/* Spreadsheet View */
.spreadsheet-view {
  border: 1px solid var(--border);
  border-radius: var(--radius);
  overflow: hidden;
}

.spreadsheet-toolbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 12px 16px;
  background: var(--bg-tertiary);
  border-bottom: 1px solid var(--border);
  flex-wrap: wrap;
  gap: 12px;
}

.toolbar-left {
  display: flex;
  align-items: center;
  gap: 8px;
}

.toolbar-left label {
  font-size: 0.9rem;
  color: var(--text-secondary);
}

.toolbar-left select {
  padding: 8px 12px;
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  background: var(--bg-secondary);
  color: var(--text-primary);
}

/* Spreadsheet Table */
.spreadsheet-table {
  overflow-x: auto;
}

.spreadsheet-header {
  display: flex;
  background: var(--bg-tertiary);
  border-bottom: 2px solid var(--border);
  min-width: 900px;
}

.spreadsheet-body {
  max-height: 500px;
  overflow-y: auto;
}

.spreadsheet-row {
  display: flex;
  border-bottom: 1px solid var(--border);
  min-width: 900px;
  transition: background 0.2s;
}

.spreadsheet-row:hover {
  background: var(--bg-tertiary);
}

.spreadsheet-row.delayed {
  background: var(--danger-light);
}

.spreadsheet-cell {
  padding: 10px 8px;
  border-right: 1px solid var(--border);
  display: flex;
  align-items: center;
  font-size: 0.9rem;
}

.spreadsheet-cell:last-child {
  border-right: none;
}

.spreadsheet-cell.header-cell {
  font-weight: 600;
  color: var(--text-secondary);
  text-transform: uppercase;
  font-size: 0.75rem;
  background: var(--bg-tertiary);
}

.spreadsheet-cell.readonly {
  background: var(--bg-tertiary);
  color: var(--text-muted);
}

.spreadsheet-cell.editable {
  cursor: pointer;
}

.spreadsheet-cell.editable:hover {
  background: var(--accent-light);
}

.spreadsheet-cell input,
.spreadsheet-cell select {
  width: 100%;
  padding: 6px 8px;
  border: 2px solid var(--accent);
  border-radius: 4px;
  background: var(--bg-secondary);
  color: var(--text-primary);
  font-size: 0.9rem;
}

.spreadsheet-cell.calculated {
  background: var(--bg-tertiary);
  font-weight: 600;
  color: var(--accent);
}

/* Cell sizes */
.cell-index { width: 40px; min-width: 40px; justify-content: center; }
.cell-name { width: 180px; min-width: 180px; }
.cell-start-date { width: 120px; min-width: 120px; }
.cell-end-date { width: 120px; min-width: 120px; }
.cell-duration { width: 80px; min-width: 80px; justify-content: center; }
.cell-progress { width: 80px; min-width: 80px; justify-content: center; }
.cell-status { width: 100px; min-width: 100px; justify-content: center; }
.cell-weight { width: 60px; min-width: 60px; justify-content: center; }
.cell-contribution { width: 90px; min-width: 90px; justify-content: center; }
.cell-notes { flex: 1; min-width: 150px; }
.cell-actions { width: 50px; min-width: 50px; justify-content: center; }

.btn-delete {
  padding: 4px;
  font-size: 0.9rem;
  background: transparent;
  border: none;
  cursor: pointer;
  opacity: 0.6;
}

.btn-delete:hover {
  opacity: 1;
}

.placeholder {
  color: var(--text-muted);
  font-style: italic;
}

/* Status badges */
.status-delayed {
  background: var(--danger-light);
  color: var(--danger);
}

[data-theme="dark"] .status-delayed {
  background: #450a0a;
  color: #f87171;
}

/* Empty state */
.empty-spreadsheet {
  padding: 48px;
  text-align: center;
  color: var(--text-muted);
}

/* Formula reference */
.formula-reference {
  padding: 16px;
  background: var(--bg-tertiary);
  border-top: 1px solid var(--border);
}

.formula-reference h4 {
  margin-bottom: 12px;
  font-size: 0.9rem;
}

.formula-reference ul {
  list-style: none;
  padding: 0;
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 8px;
}

.formula-reference li {
  font-size: 0.85rem;
  color: var(--text-secondary);
}

.formula-reference strong {
  color: var(--accent);
}

/* Add Task Form */
.add-task-form {
  padding: 20px;
  background: var(--bg-tertiary);
  border-bottom: 1px solid var(--border);
}

.add-task-form h3 {
  margin-bottom: 16px;
}

.add-task-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
  gap: 16px;
  margin-bottom: 16px;
}

/* Timeline View */
.timeline-view {
  padding: 20px;
}

.timeline-grid {
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.timeline-row {
  padding: 16px;
  background: var(--bg-tertiary);
  border-radius: var(--radius-sm);
  border-left: 4px solid var(--border);
}

.timeline-row.completed {
  border-left-color: var(--success);
}

.timeline-row.in_progress {
  border-left-color: var(--warning);
}

.timeline-row.delayed {
  border-left-color: var(--danger);
}

.timeline-row.not_started {
  border-left-color: var(--text-muted);
}

.timeline-task-name {
  font-weight: 600;
  margin-bottom: 8px;
  color: var(--text-primary);
}

.timeline-dates {
  display: flex;
  gap: 16px;
  font-size: 0.85rem;
  color: var(--text-muted);
  margin-bottom: 12px;
}

.timeline-duration {
  font-weight: 600;
  color: var(--accent);
}

.timeline-bar-container {
  height: 24px;
  background: var(--bg-secondary);
  border-radius: 12px;
  overflow: hidden;
  margin-bottom: 8px;
}

.timeline-bar {
  height: 100%;
  border-radius: 12px;
  display: flex;
  align-items: center;
  justify-content: flex-end;
  padding-right: 8px;
  min-width: 40px;
}

.timeline-bar.completed { background: var(--success); color: white; }
.timeline-bar.in_progress { background: var(--warning); color: white; }
.timeline-bar.delayed { background: var(--danger); color: white; }
.timeline-bar.not_started { background: var(--text-muted); color: white; }

.timeline-progress-label {
  font-size: 0.75rem;
  font-weight: 600;
}

.timeline-status {
  font-size: 0.85rem;
  font-weight: 500;
}

.timeline-status.completed { color: var(--success); }
.timeline-status.in_progress { color: var(--warning); }
.timeline-status.delayed { color: var(--danger); }
.timeline-status.not_started { color: var(--text-muted); }

/* Summary View */
.summary-view {
  padding: 20px;
}

.summary-panel {
  max-width: 600px;
  margin: 0 auto;
}

.summary-panel h3 {
  text-align: center;
  margin-bottom: 24px;
}

.summary-section {
  margin-bottom: 24px;
  padding: 20px;
  background: var(--bg-tertiary);
  border-radius: var(--radius);
}

.summary-section h4 {
  margin-bottom: 16px;
  font-size: 1rem;
}

/* Progress Circle */
.progress-circle-container {
  position: relative;
  width: 150px;
  height: 150px;
  margin: 0 auto;
}

.progress-circle {
  transform: rotate(-90deg);
}

.progress-circle .progress-bg {
  fill: none;
  stroke: var(--bg-secondary);
  stroke-width: 8;
}

.progress-circle .progress-fill {
  fill: none;
  stroke: var(--accent);
  stroke-width: 8;
  stroke-linecap: round;
  transition: stroke-dasharray 0.5s ease;
}

.progress-circle-text {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  text-align: center;
}

.progress-percent {
  display: block;
  font-size: 2rem;
  font-weight: 700;
  color: var(--text-primary);
}

.progress-label {
  font-size: 0.85rem;
  color: var(--text-muted);
}

/* Breakdown List */
.breakdown-list {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.breakdown-item {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 12px;
  background: var(--bg-secondary);
  border-radius: var(--radius-sm);
}

.breakdown-icon {
  font-size: 1.2rem;
}

.breakdown-label {
  flex: 1;
}

.breakdown-value {
  font-weight: 700;
  font-size: 1.1rem;
}

.breakdown-item.completed .breakdown-value { color: var(--success); }
.breakdown-item.in-progress .breakdown-value { color: var(--warning); }
.breakdown-item.delayed .breakdown-value { color: var(--danger); }
.breakdown-item.not-started .breakdown-value { color: var(--text-muted); }

/* Calc Details */
.calc-details {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.calc-row {
  display: flex;
  justify-content: space-between;
  padding: 8px;
  background: var(--bg-secondary);
  border-radius: 4px;
}

.calc-row.formula {
  flex-direction: column;
  gap: 4px;
  font-family: monospace;
  font-size: 0.9rem;
  background: var(--accent-light);
}

/* Responsive */
@media (max-width: 768px) {
  .planner-header {
    flex-direction: column;
    align-items: flex-start;
  }
  
  .planner-summary {
    flex-direction: column;
  }
  
  .spreadsheet-header,
  .spreadsheet-row {
    min-width: 700px;
  }
}

.gantt-container {
  overflow-x: auto;
  margin-top: 20px;
  border: 1px solid var(--border);
  border-radius: var(--radius);
}

.gantt-header {
  display: flex;
  background: var(--bg-tertiary);
  border-bottom: 2px solid var(--border);
  position: sticky;
  top: 0;
  z-index: 10;
}

.gantt-time-column {
  min-width: 120px;
  padding: 12px 8px;
  font-weight: 600;
  color: var(--text-secondary);
  border-right: 1px solid var(--border);
  text-align: center;
  font-size: 0.85rem;
}

.gantt-day-column {
  flex: 1;
  min-width: 100px;
  text-align: center;
  padding: 12px 8px;
  border-right: 1px solid var(--border);
}

.gantt-day-column.today {
  background: var(--accent-light);
}

.gantt-day-name {
  font-size: 0.75rem;
  color: var(--text-muted);
  text-transform: uppercase;
}

.gantt-day-number {
  font-size: 1.2rem;
  font-weight: 700;
  color: var(--accent);
}

.gantt-body {
  min-height: 300px;
}

.gantt-row {
  display: flex;
  border-bottom: 1px solid var(--border);
  min-height: 60px;
}

.gantt-row:last-child {
  border-bottom: none;
}

.gantt-row-header {
  min-width: 120px;
  padding: 12px;
  background: var(--bg-tertiary);
  border-right: 2px solid var(--border);
  display: flex;
  align-items: center;
  gap: 8px;
  position: sticky;
  left: 0;
  z-index: 5;
}

.gantt-technician-name {
  font-weight: 600;
  color: var(--text-primary);
  font-size: 0.85rem;
}

.gantt-technician-avatar {
  width: 32px;
  height: 32px;
  border-radius: 50%;
  background: linear-gradient(135deg, var(--accent) 0%, #8b5cf6 100%);
  display: flex;
  align-items: center;
  justify-content: center;
  color: white;
  font-size: 0.85rem;
  font-weight: 600;
  flex-shrink: 0;
}

.gantt-cells {
  flex: 1;
  display: flex;
  position: relative;
}

.gantt-cell {
  flex: 1;
  min-width: 100px;
  border-right: 1px solid var(--border);
  position: relative;
  min-height: 60px;
}

.gantt-cell:hover {
  background: var(--accent-light);
}

.gantt-job {
  position: absolute;
  left: 4px;
  right: 4px;
  padding: 6px 8px;
  border-radius: 6px;
  font-size: 0.75rem;
  cursor: pointer;
  overflow: hidden;
  z-index: 5;
  transition: transform 0.2s ease, box-shadow 0.2s ease;
}

.gantt-job:hover {
  transform: scale(1.02);
  box-shadow: var(--shadow-lg);
  z-index: 10;
}

.gantt-job-title {
  font-weight: 600;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.gantt-job-time {
  font-size: 0.7rem;
  opacity: 0.9;
}

.gantt-job.new { background: #f1f5f9; color: #64748b; border-left: 3px solid #64748b; }
.gantt-job.assigned { background: #dbeafe; color: #2563eb; border-left: 3px solid #2563eb; }
.gantt-job.in-progress { background: #ffedd5; color: #ea580c; border-left: 3px solid #ea580c; }
.gantt-job.completed { background: #d1fae5; color: #059669; border-left: 3px solid #059669; }

[data-theme="dark"] .gantt-job.new { background: #334155; color: #94a3b8; border-left-color: #64748b; }
[data-theme="dark"] .gantt-job.assigned { background: #1e3a5f; color: #60a5fa; border-left-color: #2563eb; }
[data-theme="dark"] .gantt-job.in-progress { background: #451a03; color: #fb923c; border-left-color: #ea580c; }
[data-theme="dark"] .gantt-job.completed { background: #064e3b; color: #34d399; border-left-color: #059669; }

/* Gantt date navigation */
.gantt-nav {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 16px;
  padding: 12px;
  background: var(--bg-tertiary);
  border-radius: var(--radius-sm);
  flex-wrap: wrap;
  gap: 12px;
}

.gantt-nav-btn {
  padding: 8px 16px;
  background: var(--bg-secondary);
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  cursor: pointer;
  font-weight: 500;
  transition: all 0.2s ease;
}

.gantt-nav-btn:hover {
  border-color: var(--accent);
  color: var(--accent);
}

.gantt-current-period {
  font-weight: 600;
  font-size: 1.1rem;
  color: var(--text-primary);
}

/* ========================================
   ACTIVITY LOG
   ======================================== */
.filter-buttons {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
}

.filter-btn {
  padding: 8px 14px;
  border: 1px solid var(--border);
  background: var(--bg-secondary);
  border-radius: 20px;
  cursor: pointer;
  font-size: 0.85rem;
  font-weight: 500;
  color: var(--text-muted);
  transition: all 0.2s ease;
}

.filter-btn.active {
  background: var(--accent);
  color: white;
  border-color: var(--accent);
}

.filter-btn:hover:not(.active) {
  border-color: var(--accent);
  color: var(--accent);
}

.activity-feed {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.activity-entry {
  display: flex;
  gap: 16px;
  padding: 16px;
  background: var(--bg-secondary);
  border-radius: var(--radius-sm);
  border: 1px solid var(--border);
  align-items: flex-start;
}

.activity-icon {
  font-size: 1.3rem;
  width: 40px;
  height: 40px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--accent-light);
  border-radius: 50%;
  flex-shrink: 0;
}

.activity-content {
  flex: 1;
}

.activity-description {
  margin-bottom: 4px;
  color: var(--text-primary);
}

.activity-meta {
  font-size: 0.8rem;
  color: var(--text-muted);
}

/* Activity Timeline */
.activity-timeline {
  position: relative;
  padding-left: 30px;
}

.activity-timeline::before {
  content: "";
  position: absolute;
  left: 10px;
  top: 0;
  bottom: 0;
  width: 2px;
  background: var(--border);
}

.activity-timeline .activity-entry {
  position: relative;
  margin-bottom: 16px;
}

.activity-timeline .activity-entry::before {
  content: "";
  position: absolute;
  left: -24px;
  top: 16px;
  width: 12px;
  height: 12px;
  border-radius: 50%;
  background: var(--accent);
  border: 2px solid var(--bg-secondary);
}

.activity-timeline .activity-entry.job::before { background: #3b82f6; }
.activity-timeline .activity-entry.customer::before { background: #10b981; }
.activity-timeline .activity-entry.invoice::before { background: #f59e0b; }
.activity-timeline .activity-entry.auth::before { background: #8b5cf6; }

.activity-filter-row {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
  margin-bottom: 20px;
  padding: 12px;
  background: var(--bg-tertiary);
  border-radius: var(--radius-sm);
}

.activity-search {
  flex: 1;
  min-width: 200px;
  padding: 10px 14px;
  border: 2px solid var(--border);
  border-radius: var(--radius-sm);
  background: var(--bg-secondary);
  color: var(--text-primary);
  font-size: 0.9rem;
}

.activity-search:focus {
  outline: none;
  border-color: var(--accent);
}

/* Quick filters */
.quick-filters {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
}

.filter-chip {
  padding: 6px 12px;
  border: 1px solid var(--border);
  background: var(--bg-secondary);
  border-radius: 20px;
  font-size: 0.8rem;
  cursor: pointer;
  transition: all 0.2s ease;
}

.filter-chip:hover {
  border-color: var(--accent);
}

.filter-chip.active {
  background: var(--accent);
  color: white;
  border-color: var(--accent);
}

/* Activity stats */
.activity-stats {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
  gap: 12px;
  margin-bottom: 20px;
}

.activity-stat {
  padding: 16px;
  background: var(--bg-tertiary);
  border-radius: var(--radius-sm);
  text-align: center;
}

.activity-stat-value {
  font-size: 1.5rem;
  font-weight: 700;
  color: var(--accent);
}

.activity-stat-label {
  font-size: 0.8rem;
  color: var(--text-muted);
}

/* ========================================
   EXPORT
   ======================================== */
.export-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 20px;
  margin-top: 20px;
}

.export-card {
  background: var(--bg-secondary);
  border-radius: var(--radius);
  padding: 28px;
  text-align: center;
  border: 1px solid var(--border);
  transition: all 0.2s ease;
}

.export-card:hover {
  transform: translateY(-2px);
  box-shadow: var(--shadow-lg);
}

.export-icon {
  font-size: 3rem;
  margin-bottom: 16px;
}

.export-card h3 {
  margin-bottom: 8px;
}

.export-card p {
  color: var(--text-muted);
  margin-bottom: 20px;
}

.export-success {
  background: var(--success-light);
  color: var(--success);
  padding: 12px 16px;
  border-radius: var(--radius-sm);
  margin-bottom: 16px;
  font-weight: 500;
}

/* ========================================
   AUTH PAGES
   ======================================== */
.auth-card {
  max-width: 420px;
  margin: 60px auto;
  padding: 36px;
}

.auth-header {
  text-align: center;
  margin-bottom: 28px;
}

.auth-header h1 {
  font-size: 2rem;
  margin-bottom: 8px;
}

.auth-header p {
  color: var(--text-muted);
}

.password-input {
  position: relative;
}

.password-input input {
  width: 100%;
  padding-right: 48px;
}

.password-input .btn-icon {
  position: absolute;
  right: 4px;
  top: 50%;
  transform: translateY(-50%);
  width: 36px;
  height: 36px;
}

.demo-accounts {
  margin-top: 24px;
  padding: 16px;
  background: var(--bg-tertiary);
  border-radius: var(--radius-sm);
}

.demo-title {
  font-size: 0.85rem;
  color: var(--text-muted);
  margin-bottom: 8px;
  text-align: center;
}

.demo-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 8px;
}

.demo-role {
  display: block;
  text-align: center;
  padding: 6px 10px;
  font-size: 0.8rem;
  border-radius: 6px;
  font-weight: 500;
}

.demo-role.admin { background: #fef3c7; color: #b45309; }
.demo-role.dispatcher { background: #dbeafe; color: #1d4ed8; }
.demo-role.technician { background: #d1fae5; color: #047857; }
.demo-role.client { background: #f3e8ff; color: #7c3aed; }

.client-portal-link {
  text-align: center;
  margin-top: 20px;
  padding-top: 20px;
  border-top: 1px solid var(--border);
}

.client-portal-link a {
  color: var(--accent);
  font-weight: 600;
}

/* ========================================
   CLIENT PORTAL
   ======================================== */
.client-stats {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
  gap: 12px;
  margin-bottom: 24px;
}

.tabs {
  display: flex;
  gap: 4px;
  border-bottom: 2px solid var(--border);
  margin-bottom: 20px;
}

.tab {
  padding: 12px 20px;
  border: none;
  background: transparent;
  cursor: pointer;
  font-size: 0.95rem;
  font-weight: 500;
  color: var(--text-muted);
  border-bottom: 2px solid transparent;
  margin-bottom: -2px;
  transition: all 0.2s ease;
}

.tab.active {
  color: var(--accent);
  border-bottom-color: var(--accent);
}

.tab:hover:not(.active) {
  color: var(--text-primary);
}

/* ========================================
   HERO SECTION
   ======================================== */
.hero-section {
  text-align: center;
  padding: 40px 20px;
  margin-bottom: 32px;
}

.hero-section h1 {
  font-size: 2.5rem;
  font-weight: 800;
  background: linear-gradient(135deg, var(--accent) 0%, #8b5cf6 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  margin-bottom: 12px;
}

.hero-subtitle {
  font-size: 1.1rem;
  color: var(--text-muted);
  max-width: 500px;
  margin: 0 auto;
}

.features-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  gap: 16px;
  margin-bottom: 32px;
}

.feature-item {
  background: var(--bg-secondary);
  border-radius: var(--radius);
  padding: 24px;
  text-align: center;
  border: 1px solid var(--border);
  transition: all 0.2s ease;
}

.feature-item:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow-lg);
}

.feature-icon {
  font-size: 2.5rem;
  margin-bottom: 12px;
}

.feature-item h3 {
  margin-bottom: 8px;
  color: var(--text-primary);
}

.feature-item p {
  color: var(--text-muted);
  font-size: 0.9rem;
}

.cta-section {
  display: flex;
  justify-content: center;
  gap: 16px;
  flex-wrap: wrap;
}

.btn-large {
  padding: 14px 28px;
  font-size: 1rem;
}

/* ========================================
   LOADING & EMPTY STATES
   ======================================== */
.loading {
  text-align: center;
  padding: 40px;
  color: var(--text-muted);
}

.loading-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 60px 20px;
}

.spinner {
  width: 48px;
  height: 48px;
  border: 4px solid var(--border);
  border-top-color: var(--accent);
  border-radius: 50%;
  animation: spin 1s linear infinite;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

.empty-state {
  text-align: center;
  padding: 48px 20px;
  color: var(--text-muted);
}

.hint {
  font-size: 0.85rem;
  margin-top: 8px;
}

/* ========================================
   DASHBOARD SECTIONS
   ======================================== */
.dashboard-section {
  margin-top: 32px;
  padding-top: 24px;
  border-top: 1px solid var(--border);
}

.dashboard-section h2 {
  margin-bottom: 16px;
}

.tech-stats-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 16px;
}

.tech-stat-card {
  display: flex;
  gap: 16px;
  padding: 16px;
  background: var(--bg-secondary);
  border-radius: var(--radius-sm);
  border: 1px solid var(--border);
}

.tech-avatar {
  width: 48px;
  height: 48px;
  background: linear-gradient(135deg, var(--accent) 0%, #8b5cf6 100%);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  color: white;
  font-size: 1.2rem;
  font-weight: 700;
}

.tech-info h4 {
  margin-bottom: 8px;
}

.tech-badges {
  display: flex;
  gap: 6px;
  flex-wrap: wrap;
}

.tech-badge {
  font-size: 0.7rem;
  padding: 2px 8px;
  border-radius: 10px;
  background: var(--bg-tertiary);
  color: var(--text-muted);
}

.tech-badge.completed { background: var(--success-light); color: var(--success); }
.tech-badge.progress { background: var(--warning-light); color: var(--warning); }

/* Category Bars */
.category-stats {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.category-bar {
  background: var(--bg-secondary);
  border-radius: var(--radius-sm);
  padding: 12px;
  border: 1px solid var(--border);
}

.category-info {
  display: flex;
  justify-content: space-between;
  margin-bottom: 8px;
}

.category-name {
  font-weight: 600;
}

.category-count {
  color: var(--text-muted);
}

.category-progress {
  height: 8px;
  background: var(--bg-tertiary);
  border-radius: 4px;
  overflow: hidden;
}

.category-fill {
  height: 100%;
  background: linear-gradient(90deg, var(--accent) 0%, #8b5cf6 100%);
  border-radius: 4px;
  transition: width 0.5s ease;
}

/* ========================================
   JOB DETAILS & PHOTOS
   ======================================== */
.job-details {
  margin-top: 16px;
  padding: 16px;
  background: var(--bg-tertiary);
  border-radius: var(--radius-sm);
}

.job-meta-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
  gap: 12px;
  margin-bottom: 16px;
}

.job-meta-grid label {
  display: flex;
  flex-direction: column;
  gap: 4px;
  font-size: 0.8rem;
  color: var(--text-muted);
}

.job-meta-grid label strong {
  font-size: 0.9rem;
  color: var(--text-primary);
}

.job-meta-grid input,
.job-meta-grid select {
  padding: 8px;
  border: 1px solid var(--border);
  border-radius: 6px;
  background: var(--bg-secondary);
  color: var(--text-primary);
}

.job-notes {
  padding: 12px;
  background: var(--accent-light);
  border-radius: 6px;
  margin-bottom: 16px;
  font-size: 0.9rem;
}

.job-photos {
  margin-bottom: 16px;
}

.photos-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(100px, 1fr));
  gap: 8px;
  margin-top: 8px;
}

.photos-grid img {
  width: 100%;
  height: 80px;
  object-fit: cover;
  border-radius: 6px;
  border: 1px solid var(--border);
}

.job-action-buttons {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
  margin-top: 16px;
  padding-top: 16px;
  border-top: 1px solid var(--border);
}

.tech-actions {
  display: flex;
  gap: 8px;
  margin-top: 12px;
  flex-wrap: wrap;
}

.completed-badge {
  padding: 8px 16px;
  background: var(--success-light);
  color: var(--success);
  border-radius: var(--radius-sm);
  font-weight: 600;
}

/* Job time estimation */
.job-duration {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  padding: 4px 8px;
  background: var(--accent-light);
  color: var(--accent);
  border-radius: 12px;
  font-size: 0.75rem;
  font-weight: 600;
}

.job-time-info {
  display: flex;
  gap: 16px;
  flex-wrap: wrap;
  margin-top: 8px;
  font-size: 0.85rem;
  color: var(--text-secondary);
}

.job-time-info span {
  display: flex;
  align-items: center;
  gap: 4px;
}

/* Job title input */
.job-id-row {
  display: flex;
  align-items: center;
  gap: 12px;
}

.job-id-row h3 {
  font-family: 'Courier New', monospace;
  font-size: 1rem;
  background: var(--bg-tertiary);
  padding: 4px 10px;
  border-radius: 6px;
}

.job-title-input {
  width: 100%;
  padding: 8px 12px;
  border: 2px solid var(--border);
  border-radius: var(--radius-sm);
  font-size: 1rem;
  background: var(--bg-secondary);
  color: var(--text-primary);
  margin: 8px 0;
}

.job-title-input:focus {
  outline: none;
  border-color: var(--accent);
}

.job-customer {
  font-size: 0.9rem;
  color: var(--text-secondary);
  margin: 8px 0;
}

/* ========================================
   SIDEBAR (Mobile Navigation)
   ======================================== */
.sidebar-toggle {
  display: none;
  background: none;
  border: none;
  font-size: 1.5rem;
  cursor: pointer;
  padding: 4px 8px;
  color: var(--text-primary);
}

/* Sidebar overlay */
.sidebar-overlay {
  display: none;
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  z-index: 99;
}

.sidebar-overlay.open {
  display: block;
}

/* Sidebar */
.sidebar {
  display: none;
}

/* ========================================
   RESPONSIVE - Tablet and below
   ======================================== */
@media (max-width: 1024px) {
  .main {
    padding: 24px 16px;
  }
  
  .stats-grid {
    grid-template-columns: repeat(3, 1fr);
  }
  
  .stat-card.large {
    grid-column: span 3;
  }
}

/* ========================================
   RESPONSIVE - Mobile Landscape & Tablet Portrait
   ======================================== */
@media (max-width: 768px) {
  /* Reset and base mobile styles */
  * {
    -webkit-tap-highlight-color: transparent;
  }
  
  .sidebar-toggle {
    display: block;
  }
  
  .topbar {
    padding: 10px 12px;
    gap: 8px;
    min-height: 52px;
  }
  
  .brand-section {
    display: flex;
    align-items: center;
    gap: 8px;
    flex: 1;
    min-width: 0;
  }
  
  .brand {
    font-size: 0.95rem;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }
  
  .nav-links {
    display: none;
  }
  
  .user-section {
    display: flex;
    align-items: center;
    gap: 6px;
    flex-shrink: 0;
  }
  
  .user-menu {
    font-size: 0.7rem;
    padding: 4px 6px;
    gap: 4px;
  }
  
  .user-name {
    display: none;
  }
  
  .user-role {
    font-size: 0.6rem;
    padding: 1px 4px;
  }
  
  .btn-icon {
    width: 36px;
    height: 36px;
    font-size: 1rem;
  }
  
  /* Show sidebar on mobile */
  .sidebar {
    display: block;
    position: fixed;
    top: 0;
    left: -85%;
    width: 85%;
    max-width: 300px;
    height: 100vh;
    background: var(--bg-secondary);
    z-index: 100;
    transition: left 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    box-shadow: 0 0 50px rgba(0, 0, 0, 0.3);
    overflow-y: auto;
    -webkit-overflow-scrolling: touch;
  }
  
  .sidebar.open {
    left: 0;
  }
  
  .sidebar-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 16px;
    border-bottom: 1px solid var(--border);
    background: var(--bg-tertiary);
    position: sticky;
    top: 0;
    z-index: 10;
  }
  
  .sidebar-brand {
    font-size: 1rem;
    font-weight: 700;
    color: var(--accent);
    text-decoration: none;
    white-space: nowrap;
  }
  
  .sidebar-close {
    background: none;
    border: none;
    font-size: 1.5rem;
    cursor: pointer;
    color: var(--text-secondary);
    padding: 4px 8px;
    width: 36px;
    height: 36px;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  
  .sidebar-nav {
    padding: 8px 0;
  }
  
  .sidebar-nav a {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 14px 16px;
    text-decoration: none;
    color: var(--text-secondary);
    font-weight: 500;
    transition: all 0.15s ease;
    border-left: 3px solid transparent;
    font-size: 0.95rem;
  }
  
  .sidebar-nav a:hover,
  .sidebar-nav a:active {
    background: var(--accent-light);
    color: var(--accent);
  }
  
  .sidebar-nav a.active {
    background: var(--accent-light);
    color: var(--accent);
    border-left-color: var(--accent);
    font-weight: 600;
  }
  
  .sidebar-user {
    padding: 16px;
    border-top: 1px solid var(--border);
    background: var(--bg-tertiary);
    position: sticky;
    bottom: 0;
  }
  
  .sidebar-user-info {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 12px;
  }
  
  .sidebar-user-avatar {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--accent), #8b5cf6);
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-weight: 700;
    font-size: 1rem;
  }
  
  .sidebar-user-name {
    font-weight: 600;
    color: var(--text-primary);
    font-size: 0.95rem;
  }
  
  .sidebar-user-role {
    font-size: 0.7rem;
    padding: 2px 8px;
    background: var(--accent-light);
    color: var(--accent);
    border-radius: 8px;
    text-transform: capitalize;
  }
  
  .sidebar-user-actions {
    display: flex;
    gap: 8px;
  }
  
  .sidebar-user-actions button {
    flex: 1;
    padding: 10px;
    font-size: 0.9rem;
  }
  
  /* Sidebar overlay */
  .sidebar-overlay {
    display: block;
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.5);
    z-index: 99;
    opacity: 0;
    visibility: hidden;
    transition: opacity 0.3s ease, visibility 0.3s ease;
    -webkit-backdrop-filter: blur(2px);
  }
  
  .sidebar-overlay.open {
    opacity: 1;
    visibility: visible;
  }
  
  /* Main content */
  .main {
    padding: 16px 12px;
  }
  
  .card {
    padding: 16px;
    border-radius: 10px;
    margin-bottom: 16px;
  }
  
  .card h1 {
    font-size: 1.4rem;
  }
  
  .card h2 {
    font-size: 1.1rem;
  }
  
  /* Stats */
  .stats-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 10px;
  }
  
  .stat-card {
    padding: 14px;
    gap: 10px;
  }
  
  .stat-card.large {
    grid-column: span 2;
  }
  
  .stat-icon {
    width: 44px;
    height: 44px;
    font-size: 1.5rem;
  }
  
  .stat-value {
    font-size: 1.4rem;
  }
  
  .stat-label {
    font-size: 0.75rem;
  }
  
  /* Page header */
  .page-header {
    flex-direction: column;
    gap: 12px;
  }
  
  .page-header h1 {
    font-size: 1.3rem;
    flex-wrap: wrap;
  }
  
  /* Forms */
  .form-section {
    padding: 16px;
  }
  
  .form-grid {
    grid-template-columns: 1fr;
    gap: 12px;
  }
  
  .form-group input,
  .form-group select,
  .form-group textarea {
    padding: 10px 12px;
    font-size: 0.9rem;
  }
  
  .form-actions {
    flex-direction: column;
  }
  
  .form-actions button {
    width: 100%;
  }
  
  /* Buttons */
  .btn-primary,
  .btn-secondary,
  .btn-success,
  .btn-danger {
    padding: 12px 16px;
    font-size: 0.9rem;
  }
  
  /* Auth */
  .auth-card {
    margin: 16px;
    padding: 20px;
    border-radius: 12px;
  }
  
  .auth-header h1 {
    font-size: 1.5rem;
  }
  
  /* Hero */
  .hero-section {
    padding: 24px 12px;
  }
  
  .hero-section h1 {
    font-size: 1.6rem;
  }
  
  .hero-subtitle {
    font-size: 0.95rem;
  }
  
  .features-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 10px;
  }
  
  .feature-item {
    padding: 16px 12px;
  }
  
  .feature-icon {
    font-size: 1.8rem;
  }
  
  .feature-item h3 {
    font-size: 0.9rem;
  }
  
  .feature-item p {
    font-size: 0.8rem;
  }
  
  .cta-section {
    flex-direction: column;
    gap: 10px;
  }
  
  .btn-large {
    width: 100%;
    text-align: center;
  }
  
  /* Search */
  .search-bar,
  .search-filter-bar {
    flex-direction: column;
    gap: 10px;
  }
  
  .search-bar input,
  .search-filter-bar input,
  .search-filter-bar select {
    width: 100%;
    min-width: 0;
  }
  
  /* Tables */
  .gantt-header {
    min-width: 550px;
  }

  .gantt-row {
    min-width: 550px;
  }
  
  /* Invoice */
  .invoice-summary {
    grid-template-columns: 1fr;
    gap: 10px;
  }
  
  /* Job counts */
  .job-counts {
    gap: 8px;
  }
  
  .job-counts .count {
    font-size: 0.75rem;
    padding: 3px 8px;
  }
  
  /* Items */
  .items-grid,
  .jobs-list {
    gap: 12px;
  }
  
  .item-card,
  .job-item {
    padding: 14px;
  }
  
  .item-header h3,
  .job-header h3 {
    font-size: 1rem;
  }
  
  /* Project cards */
  .projects-grid {
    grid-template-columns: 1fr;
    gap: 16px;
  }
  
  /* Kanban */
  .kanban-board {
    grid-template-columns: 1fr;
  }
  
  /* Modal */
  .modal-content {
    margin: 12px;
    max-height: calc(100vh - 24px);
  }
  
  .modal-header {
    padding: 16px;
  }
  
  .modal-content .tab-content {
    padding: 16px;
  }
}

/* ========================================
   RESPONSIVE - Small Mobile
   ======================================== */
@media (max-width: 400px) {
  .topbar {
    padding: 8px 10px;
    gap: 6px;
  }
  
  .brand {
    font-size: 0.85rem;
  }
  
  .sidebar-toggle {
    font-size: 1.3rem;
    padding: 2px 6px;
  }
  
  .btn-icon {
    width: 32px;
    height: 32px;
  }
  
  .user-menu {
    padding: 3px 5px;
  }
  
  .user-role {
    display: none;
  }
  
  .stats-grid {
    grid-template-columns: 1fr;
  }
  
  .stat-card.large {
    grid-column: span 1;
  }
  
  .features-grid {
    grid-template-columns: 1fr;
  }
  
  .auth-card {
    margin: 12px;
    padding: 16px;
  }
  
  .demo-grid {
    grid-template-columns: 1fr;
  }
  
  .gantt-header {
    min-width: 400px;
  }

  .gantt-row {
    min-width: 400px;
  }
}

/* ========================================
   RESPONSIVE - Large Desktop
   ======================================== */
@media (min-width: 1400px) {
  .main {
    max-width: 1400px;
    padding: 40px 32px;
  }
  
  .stats-grid {
    grid-template-columns: repeat(6, 1fr);
  }
  
  .stat-card.large {
    grid-column: span 2;
  }
  
  .features-grid {
    grid-template-columns: repeat(4, 1fr);
  }
}

/* ========================================
   RESPONSIVE - Extra Small Devices
   ======================================== */
@media (max-width: 320px) {
  .topbar {
    padding: 6px 8px;
  }
  
  .brand {
    font-size: 0.75rem;
  }
  
  .main {
    padding: 12px 8px;
  }
  
  .card {
    padding: 12px;
  }
  
  .card h1 {
    font-size: 1.2rem;
  }
  
  .hero-section h1 {
    font-size: 1.4rem;
  }
}

/* ========================================
   PROJECTS PAGE
   ======================================== */
.project-stats {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
  gap: 16px;
  margin-bottom: 24px;
}

.header-actions {
  display: flex;
  gap: 16px;
  align-items: center;
  flex-wrap: wrap;
}

/* Project Cards - Grid View */
.projects-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
  gap: 20px;
}

.project-card {
  background: var(--bg-secondary);
  border-radius: var(--radius);
  padding: 20px;
  border: 1px solid var(--border);
  transition: all 0.2s ease;
}

.project-card:hover {
  transform: translateY(-2px);
  box-shadow: var(--shadow-lg);
  border-color: var(--accent);
}

.project-header {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  margin-bottom: 12px;
  gap: 12px;
}

.project-header h3 {
  font-size: 1.1rem;
  font-weight: 600;
  flex: 1;
  color: var(--text-primary);
}

.project-description {
  color: var(--text-secondary);
  font-size: 0.9rem;
  margin-bottom: 16px;
  line-height: 1.5;
}

.project-progress {
  margin-bottom: 16px;
}

.progress-header {
  display: flex;
  justify-content: space-between;
  margin-bottom: 8px;
  font-size: 0.85rem;
  color: var(--text-muted);
}

.progress-bar {
  height: 8px;
  background: var(--bg-tertiary);
  border-radius: 4px;
  overflow: hidden;
}

.progress-fill {
  height: 100%;
  background: linear-gradient(90deg, var(--accent) 0%, #8b5cf6 100%);
  border-radius: 4px;
  transition: width 0.5s ease;
}

.project-meta {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
  font-size: 0.8rem;
  color: var(--text-muted);
  margin-bottom: 16px;
}

.project-actions {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
  padding-top: 16px;
  border-top: 1px solid var(--border);
}

/* Projects List View */
.projects-list {
  overflow-x: auto;
}

.data-table {
  width: 100%;
  border-collapse: collapse;
  background: var(--bg-secondary);
  border-radius: var(--radius);
  overflow: hidden;
}

.data-table th,
.data-table td {
  padding: 14px 16px;
  text-align: left;
  border-bottom: 1px solid var(--border);
}

.data-table th {
  background: var(--bg-tertiary);
  font-weight: 600;
  font-size: 0.85rem;
  color: var(--text-secondary);
  text-transform: uppercase;
}

.data-table tr:hover {
  background: var(--bg-tertiary);
}

.mini-progress {
  display: flex;
  align-items: center;
  gap: 8px;
  min-width: 100px;
}

.mini-progress .mini-progress-fill {
  flex: 1;
  height: 6px;
  background: var(--bg-tertiary);
  border-radius: 3px;
  overflow: hidden;
}

.mini-progress .mini-progress-fill {
  height: 100%;
  background: linear-gradient(90deg, var(--accent) 0%, #8b5cf6 100%);
}

.mini-progress span {
  font-size: 0.8rem;
  font-weight: 600;
  min-width: 35px;
}

/* Kanban Board */
.kanban-board {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 20px;
}

.kanban-column {
  background: var(--bg-tertiary);
  border-radius: var(--radius);
  padding: 16px;
  min-height: 400px;
}

.kanban-column h3 {
  font-size: 0.95rem;
  font-weight: 600;
  color: var(--text-secondary);
  margin-bottom: 16px;
  padding-bottom: 12px;
  border-bottom: 2px solid var(--border);
}

.kanban-cards {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.kanban-card {
  background: var(--bg-secondary);
  border-radius: var(--radius-sm);
  padding: 16px;
  border: 1px solid var(--border);
  cursor: pointer;
  transition: all 0.2s ease;
}

.kanban-card:hover {
  transform: translateY(-2px);
  box-shadow: var(--shadow);
  border-color: var(--accent);
}

.kanban-card.completed {
  opacity: 0.7;
}

.kanban-card h4 {
  font-size: 0.95rem;
  font-weight: 600;
  margin-bottom: 8px;
  color: var(--text-primary);
}

.kanban-meta {
  display: flex;
  justify-content: space-between;
  align-items: center;
  font-size: 0.8rem;
  color: var(--text-muted);
}

.kanban-progress {
  display: flex;
  align-items: center;
  gap: 8px;
}

.kanban-progress .progress-bar {
  flex: 1;
  height: 4px;
}

/* Project Modal */
.modal-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.6);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
  padding: 20px;
}

.modal-content {
  background: var(--bg-secondary);
  border-radius: var(--radius);
  width: 100%;
  max-width: 700px;
  max-height: 90vh;
  overflow-y: auto;
  box-shadow: var(--shadow-lg);
}

.modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20px 24px;
  border-bottom: 1px solid var(--border);
}

.modal-header h2 {
  font-size: 1.3rem;
  margin: 0;
}

.modal-content .tabs {
  padding: 0 24px;
  margin-top: 16px;
}

.modal-content .tab-content {
  padding: 24px;
}

/* Project Details in Modal */
.detail-row {
  display: flex;
  justify-content: space-between;
  padding: 12px 0;
  border-bottom: 1px solid var(--border);
}

.detail-row:last-child {
  border-bottom: none;
}

.detail-row span:first-child {
  font-weight: 600;
  color: var(--text-secondary);
}

.detail-row span:last-child {
  color: var(--text-primary);
}

/* Tasks */
.tasks-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 16px;
}

.tasks-header h3 {
  font-size: 1.1rem;
  margin: 0;
}

.task-form {
  margin-bottom: 20px;
  padding: 16px;
  background: var(--bg-tertiary);
  border-radius: var(--radius-sm);
}

.tasks-list {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.task-item {
  background: var(--bg-secondary);
  border-radius: var(--radius-sm);
  padding: 16px;
  border: 1px solid var(--border);
}

.task-item.completed {
  opacity: 0.7;
}

.task-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 8px;
}

.task-header h4 {
  font-size: 0.95rem;
  font-weight: 600;
  color: var(--text-primary);
}

.task-item > p {
  font-size: 0.9rem;
  color: var(--text-secondary);
  margin-bottom: 12px;
}

.task-meta {
  display: flex;
  gap: 16px;
  font-size: 0.8rem;
  color: var(--text-muted);
  margin-bottom: 12px;
}

.task-progress {
  margin-bottom: 12px;
}

.task-actions {
  display: flex;
  gap: 8px;
}

/* Status badges for projects */
.status-overdue {
  background: #fee2e2;
  color: #dc2626;
}

[data-theme="dark"] .status-overdue {
  background: #450a0a;
  color: #f87171;
}

/* Priority badges */
.priority-badge {
  display: inline-block;
  padding: 2px 8px;
  border-radius: 10px;
  font-size: 0.7rem;
  font-weight: 600;
  text-transform: uppercase;
}

.priority-badge.high { background: #fee2e2; color: #dc2626; }
.priority-badge.medium { background: #fef3c7; color: #d97706; }
.priority-badge.low { background: #f1f5f9; color: #64748b; }

[data-theme="dark"] .priority-badge.high { background: #450a0a; color: #f87171; }
[data-theme="dark"] .priority-badge.medium { background: #451a03; color: #fbbf24; }
[data-theme="dark"] .priority-badge.low { background: #334155; color: #94a3b8; }

/* ========================================
   EXCEL-STYLE PROJECT PLANNER
   ======================================== */

/* Excel-style planner container */
.project-planner.excel-style {
  overflow-x: auto;
}

/* Hierarchy level styling */
.spreadsheet-row.level-0 {
  background: var(--bg-secondary);
  font-weight: 600;
}

.spreadsheet-row.level-1 {
  padding-left: 24px;
  background: var(--bg-tertiary);
}

.spreadsheet-row.level-2 {
  padding-left: 48px;
  background: var(--bg-primary);
}

/* Task number column */
.cell-number {
  width: 50px;
  min-width: 50px;
  justify-content: center;
  font-family: 'Courier New', monospace;
  font-weight: 600;
  color: var(--accent);
}

/* Timeline bar cell */
.cell-timeline {
  flex: 1;
  min-width: 200px;
  padding: 4px 8px;
}

.timeline-bar-cell {
  position: relative;
  width: 100%;
  height: 28px;
}

.timeline-bar-track {
  position: relative;
  width: 100%;
  height: 100%;
  background: var(--bg-tertiary);
  border-radius: 4px;
  overflow: hidden;
}

.timeline-bar-fill {
  position: absolute;
  top: 2px;
  bottom: 2px;
  border-radius: 3px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 0.7rem;
  font-weight: 600;
  color: white;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  padding: 0 4px;
  min-width: 30px;
  transition: all 0.3s ease;
}

.timeline-bar-fill.completed {
  background: linear-gradient(90deg, var(--success) 0%, #059669 100%);
}

.timeline-bar-fill.in_progress {
  background: linear-gradient(90deg, var(--warning) 0%, #d97706 100%);
}

.timeline-bar-fill.delayed {
  background: linear-gradient(90deg, var(--danger) 0%, #dc2626 100%);
}

.timeline-bar-fill.not_started {
  background: var(--text-muted);
}

/* Excel legend */
.excel-legend {
  display: flex;
  gap: 16px;
  padding: 12px 16px;
  background: var(--bg-tertiary);
  border-top: 1px solid var(--border);
  font-size: 0.85rem;
  flex-wrap: wrap;
}

.excel-legend span {
  color: var(--text-secondary);
}

/* Simple view */
.simple-view {
  padding: 16px;
}

.simple-tasks-list {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.simple-task-card {
  background: var(--bg-secondary);
  border-radius: var(--radius-sm);
  padding: 16px;
  border: 1px solid var(--border);
}

.simple-task-card.level-1 {
  margin-left: 20px;
  border-left: 3px solid var(--accent);
}

.simple-task-card.level-2 {
  margin-left: 40px;
  border-left: 3px solid var(--text-muted);
}

.simple-task-header {
  display: flex;
  align-items: center;
  gap: 12px;
  margin-bottom: 8px;
}

.simple-task-header .task-number {
  font-family: 'Courier New', monospace;
  font-weight: 600;
  color: var(--accent);
  min-width: 50px;
}

.simple-task-header .task-name {
  flex: 1;
  font-weight: 600;
}

.simple-task-dates {
  display: flex;
  gap: 8px;
  font-size: 0.85rem;
  color: var(--text-muted);
  margin-bottom: 8px;
}

.simple-task-dates .duration {
  font-weight: 600;
  color: var(--accent);
}

.simple-task-progress {
  margin-bottom: 8px;
}

.simple-task-actions {
  display: flex;
  justify-content: flex-end;
}

/* Project select in planner header */
.project-select {
  padding: 8px 12px;
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  background: var(--bg-secondary);
  color: var(--text-primary);
  font-size: 0.9rem;
}

/* Planner actions */
.planner-actions {
  display: flex;
  gap: 12px;
}

/* Planner timeline v2 */
.fp-shell {
  display: grid;
  gap: 10px;
}

.fp-toolbar {
  display: flex;
  align-items: center;
  gap: 10px;
  justify-content: space-between;
}

.fp-toolbar select {
  min-width: 220px;
}

.fp-error {
  padding: 8px 10px;
  border-radius: 8px;
  background: #fee2e2;
  color: #991b1b;
}

.fp-layout {
  display: grid;
  grid-template-columns: minmax(360px, 42%) 1fr;
  gap: 10px;
  min-height: 70vh;
}

.fp-left,
.fp-right {
  border: 1px solid var(--stroke);
  border-radius: 10px;
  background: #fff;
  overflow: hidden;
}

.fp-create {
  display: grid;
  grid-template-columns: 1.4fr 1fr 1fr 0.8fr auto;
  gap: 8px;
  padding: 10px;
  border-bottom: 1px solid var(--stroke);
}

.fp-create input,
.fp-create select,
.fp-create button,
.fp-task-row input {
  border: 1px solid var(--stroke);
  border-radius: 8px;
  padding: 7px 9px;
  font: inherit;
}

.fp-create button {
  background: var(--brand);
  color: #fff;
  border-color: var(--brand);
}

.fp-task-table {
  overflow: auto;
  max-height: calc(70vh - 64px);
}

.fp-task-row {
  display: grid;
  grid-template-columns: 1.2fr 1fr 1fr 72px 72px 84px;
  gap: 6px;
  padding: 8px;
  border-bottom: 1px solid var(--stroke);
}

.fp-metrics {
  font-size: 0.78rem;
  color: var(--muted);
  display: flex;
  align-items: center;
}

.fp-hours {
  overflow: auto;
  height: 100%;
  position: relative;
}

.fp-hours-inner {
  min-height: 100%;
  position: relative;
  background-image: repeating-linear-gradient(
    to right,
    rgba(15, 118, 110, 0.08) 0,
    rgba(15, 118, 110, 0.08) 1px,
    transparent 1px,
    transparent 64px
  );
}

.fp-hours-header {
  position: sticky;
  top: 0;
  z-index: 5;
  display: flex;
  background: #f8fafc;
  border-bottom: 1px solid var(--stroke);
}

.fp-hour {
  flex: 0 0 auto;
  padding: 8px 6px;
  font-size: 0.72rem;
  color: var(--muted);
  border-right: 1px solid #edf2f7;
  white-space: nowrap;
}

.fp-now {
  position: absolute;
  top: 0;
  bottom: 0;
  width: 2px;
  background: #ef4444;
  z-index: 4;
}

.fp-track-row {
  position: relative;
  height: 74px;
  border-bottom: 1px solid #f1f5f9;
}

.fp-bar {
  position: absolute;
  top: 12px;
  height: 50px;
  border-radius: 10px;
  overflow: hidden;
  border: 1px solid rgba(0, 0, 0, 0.08);
  display: flex;
  flex-direction: column;
  justify-content: center;
  padding: 4px 8px;
}

.fp-bar-fill {
  position: absolute;
  left: 0;
  top: 0;
  bottom: 0;
  background: rgba(255, 255, 255, 0.26);
}

.fp-bar-label,
.fp-bar-time {
  position: relative;
  z-index: 1;
  color: #fff;
  white-space: nowrap;
  text-overflow: ellipsis;
  overflow: hidden;
}

.fp-bar-label {
  font-size: 0.8rem;
  font-weight: 700;
}

.fp-bar-time {
  font-size: 0.68rem;
}

.fp-status-scheduled { background: #475569; }
.fp-status-progress { background: #0ea5e9; }
.fp-status-paused { background: #f59e0b; }
.fp-status-completed { background: #64748b; opacity: 0.7; }

.fp-actions {
  position: absolute;
  right: 8px;
  bottom: 8px;
  display: flex;
  gap: 6px;
}

.fp-actions button {
  border: 1px solid var(--stroke);
  border-radius: 7px;
  background: #fff;
  font-size: 0.72rem;
  padding: 4px 8px;
}

.fp-readonly {
  color: var(--muted);
  font-size: 0.72rem;
}

@media (max-width: 1080px) {
  .fp-layout {
    grid-template-columns: 1fr;
  }

  .fp-create {
    grid-template-columns: 1fr;
  }

  .fp-task-row {
    grid-template-columns: 1fr;
  }
}
